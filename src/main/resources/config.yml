# Syziege 웹맵 설정
webmap:
  # 웹맵 활성화 여부
  enabled: true
  # 웹 서버 바인드 주소 (0.0.0.0 = 모든 인터페이스)
  bind: 0.0.0.0
  # 웹 서버 포트
  port: 8123
  # 웹으로 노출할 월드 목록 (비워두면 모든 월드)
  worlds: []
  # 타일 캐시 유지 시간(초). 리전 파일이 변경되면 이 시간이 지난 뒤 다시 렌더링됩니다.
  tile-cache-seconds: 30
